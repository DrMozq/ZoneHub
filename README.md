# ZoneHub 📍

**ZoneHub** — это программно-аппаратный комплекс для мониторинга перемещения объектов внутри помещений в реальном времени (RTLS).

## 🚀 Суть проекта
Система использует шлюзы на базе **ESP32**, которые сканируют Bluetooth Low Energy (BLE) метки и передают данные по протоколу **MQTT** на центральный сервер. Сервер определяет текущую зону нахождения объекта и отображает данные в веб-интерфейсе.

## 🛠 Технологический стек
- **Hardware:** ESP32 (C++, Arduino framework, BLE stack).
- **Backend:** Python 3, MQTT (Mosquitto).
- **Database:** SQLite.
- **Web UI:** Flask, Bootstrap 5.
- **DevOps:** Loguru (logging), Dotenv (config).

## 📋 Структура проекта
- `/esp32_firmware` — прошивка для шлюзов.
- `mqtt_handler.py` — сервис обработки данных и записи в БД.
- `run_web.py` — веб-сервер Flask.
- `database.py` — уровень работы с данными.
- `settings.py` — конфигурация системы и логирование.